<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">All Posts</h1>
</div>
<table class="table table-striped  table-hover">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">Content</th>
            <th scope="col">Allow Comment</th>
            <th scope="col">Status</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        {{#each posts }}
            <tr>
                <td>{{title}}</td>
                <td>{{desc}}</td>
                <td>{{content}}</td>
                <td>{{allow_comments}}</td>
                <td>{{status}}</td>
                <td class="d-flex">
                    <a href="/admin/posts/edit/{{_id}}" class="btn btn-sm btn-success mr-2"><i class="fa fa-fw fa-edit"></i></a>
                    <form action="/admin/posts/delete/{{_id}}?newMethod=DELETE" method="post">
                        <button type="submit" class="btn btn-sm btn-danger"><i class="fa fa-fw fa-trash"></i></button>
                    </form>
                </td>
            </tr>
        {{/each}}
    </tbody>
</table>
